# Spring properties
spring:
  data:
    mongodb:
      host: 172.17.0.1
      port: 27017
      uri: mongodb://172.17.0.1/test
  application:
    name: user-service  # Service registers under this name

endpoints:
  health:
    enabled: yes
  shutdown:
    enabled: yes

management:
    context-path: /admin
logging:
  level:
    org:
      springframework:
        web:
          client: DEBUG

# HTTP Server
server:
  port: 4444   # HTTP (Tomcat) port

